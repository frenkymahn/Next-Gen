name: Build Xiaomi 11T Pro Standalone Boot Image

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt update
          sudo apt install -y bc bison build-essential ccache curl flex git gnupg gperf libelf-dev liblz4-tool libncurses5-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 python3-pip device-tree-compiler

      - name: Fetch Compiler Toolchain (Proton Clang)
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang

      - name: Fetch Kernel Source Code
        run: |
          rm -rf kernel
          git clone --depth=1 https://github.com/frenkymahn/Next-Gen.git kernel

      - name: Run Compilation Process (Kernel & DTBs)
        run: |
          cd kernel
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          
          BUILD_FLAGS="ARCH=arm64 SUBARCH=arm64 CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- CLANG_TRIPLE=aarch64-linux-gnu- LLVM=1 LLVM_IAS=1 KCFLAGS=-Wno-error -Wno-unknown-warning-option -Wno-ignored-optimization-argument"
          
          make O=out $BUILD_FLAGS vili_defconfig

          # Force Permissive mode to bypass bootloop
          scripts/config --file out/.config --enable CONFIG_ALWAYS_PERMISSIVE
          scripts/config --file out/.config --disable CONFIG_SECURITY_SELINUX_DEVELOP

          # Compile Kernel Image and DTBs
          make -j$(nproc --all) O=out $BUILD_FLAGS
          make -j$(nproc --all) O=out $BUILD_FLAGS dtbs

      - name: Create Standalone boot.img
        run: |
          mkdir -p output_boot
          git clone --depth=1 https://android.googlesource.com/platform/system/tools/mkbootimg tools/mkbootimg

          # Locate kernel binary
          if [ -f kernel/out/arch/arm64/boot/Image.gz ]; then
            KERNEL_BIN="kernel/out/arch/arm64/boot/Image.gz"
          elif [ -f kernel/out/arch/arm64/boot/Image ]; then
            KERNEL_BIN="kernel/out/arch/arm64/boot/Image"
          else
            echo "ERROR: Kernel binary not found!"
            exit 1
          fi

          # Build Xiaomi 11T Pro Header v3 Boot Image
          python3 tools/mkbootimg/mkbootimg.py \
            --kernel "$KERNEL_BIN" \
            --header_version 3 \
            --pagesize 4096 \
            --cmdline "console=null androidboot.hardware=qcom androidboot.memcg=1 lpm_levels.sleep_disabled=1 service_locator.enable=1 androidboot.usbcontroller=a600000.dwc3 swiotlb=2048 loop.max_part=7 firmware_class.path=/vendor/firmware_mnt/image androidboot.selinux=permissive" \
            --os_version 14.0.0 \
            --os_patch_level 2026-05 \
            -o output_boot/boot.img

      - name: Upload Boot Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Xiaomi-11T-Pro-Standalone-Boot
          path: output_boot/boot.img
